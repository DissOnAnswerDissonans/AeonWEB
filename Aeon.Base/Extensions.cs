using System;
using System.Collections.Generic;
using System.Text;

namespace Aeon.Base
{
	public static class Extensions
	{
		//public static T FlagOn<T>(this T @enum, T flags) where T: Enum
		//{
		//	return (T) Enum.Parse(typeof(T), (((int) @enum.GetTypeCode()) | ((int) flags.GetTypeCode())).ToString());
		//}
	}
}
